// Fibonacci function example
fibonacci(n) {
    if (n <= 1) {
        return n;
    } else {
        return fibonacci(n - 1) + fibonacci(n - 2);
    }
}

// Main function
main() {
    var num = 8;
    var result = fibonacci(num);
    return result;
}