// Factorial function example
factorial(n) {
    var result = 1;
    var i = 1;
    
    while (i <= n) {
        result = result * i;
        i = i + 1;
    }
    
    return result;
}

// Main function
main() {
    var num = 5;
    var fact = factorial(num);
    return fact;
}