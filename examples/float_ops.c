// Example with floating point operations
double calculate(double x, double y) {
    double result = x * 2.5 + y / 3.0;
    return result;
}

int main() {
    double a = 10.5;
    double b = 20.75;
    double c = calculate(a, b);
    return 0;
}
